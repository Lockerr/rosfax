.photos{:style => 'overflow-y: scroll;height: 500px'}
  %ul.nav.nav-tabs
    %li.active
      %a{"data-toggle" => "tab", :href => "#foo"} unass
    %li
      %a{"data-toggle" => "tab", :href => "#bar"} all
  .tab-content
    #foo.tab-pane.active
      - for asset in @report.assets.where('id not in (?)', @report.assigned.map {|i| i.to_i})
        = image_tag asset.url(:thumb), :draggable => 'true',:id => asset.id, :style => 'cursor: move;'
    #bar.tab-pane
      - for asset in @report.assets
        = image_tag asset.url(:thumb), :draggable => 'true',:id => asset.id, :style => 'cursor: move;'
%hr/
.files
  = form_for [@report, Asset.new], :html => { :class => "upload", :multipart => true } do |f|
    = f.file_field :data
    %div Перетащить сюда файлы
  %table.upload_files
  -#%table.download_files
%p
  %b Car:
  = @report.car_id

= link_to 'Edit', edit_report_path(@report)
\|
= link_to 'Back', reports_path