%p#notice= notice
.row-fluid
  .span12
    .tabbable.tabs-left
      %ul.nav.nav-tabs.first
        %li.active
          %a{"data-toggle" => "tab", :href => "#info"}
            Информация
            %span{:style => 'color: #ddd'}= @report.counters[:car][:summ][2]
        %li
          %a{"data-toggle" => "tab", :href => "#photo"}
            Фото
            %span{:style => 'color: #ddd'}= @report.counters[:photo][:summ][2]
        %li
          %a{"data-toggle" => "tab", :href => "#checklist"}
            Чеклист
            %span{:style => 'color: #ddd'}= @report.counters[:checklist][:summ][2]
        %li
          %a{"data-toggle" => "tab", :href => "#testdrive"}
            Тест-драйв
            %span{:style => 'color: #ddd'}= @report.counters[:testdrive][:summ][2]
        %li
          %a{"data-toggle" => "tab", :href => "#elements"}
            Элементы
            %span{:style => 'color: #ddd'}= "#{@report.elements.count}/#{Report::DEFECTS_CATEGORIES.values.flatten.size}"
      .tab-content.first
        - for symbol in %w(photo checklist defects testdrive elements info)
          = render "reports/tabs/#{symbol}"
.span4.uploader.hide
  = render :partial => 'uploader', :locals => {:attribute => ''}

#modal_carousel.modal.hide{:style => 'overflow: hidden'}
  %button.close{'data-dismiss'=>"modal"}
  #myCarousel.carousel.slide{:style => 'padding: 5px'}
    .carousel-inner
      - for image in @report.images :group => 'visual_interior', :size => 'carousel'
        .item{:id => "active_#{image[0]}"}
          = image_tag(image[1])

    %a.carousel-control.left{"data-slide" => "prev", :href => "#myCarousel"} ‹
    %a.carousel-control.right{"data-slide" => "next", :href => "#myCarousel"} ›
    %a.carousel-control.close.top-left{'data-dismiss' => 'modal', :href => "#myCarousel", :style => 'width: 30px, height: 30px; font-size: 34px; top: 6%; left: 91%'} x

