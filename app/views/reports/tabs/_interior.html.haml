- placements = Report::INTERIOR
.row-fluid{:id => 'interior'}

  .span8
    %ul.thumbnails
      - for place in placements[0..2]
        = render :partial => 'drop', :locals => {:object => @report, :place => place, :attribute => 'interior'}
    %ul.thumbnails
      - for place in placements[3..5]
        = render :partial => 'drop', :locals => {:object => @report, :place => place, :attribute => 'interior'}

    %ul.thumbnails
      - for place in placements[6..8]
        = render :partial => 'drop', :locals => {:object => @report, :place => place, :attribute => 'interior'}
    .span{:style => "height: 96px; overflow-x: scroll; width: 98%; overflow-y: hidden;border: 1px solid #08C"}
      .imgbox#interior{:style => "height: 110px; width: #{@report.images(:group => 'interior', :size => 'thumb').size*116}px", :data => {:attribute => 'interior', :place => placements.first}}
        - for image in @report.images(:group => 'interior', :size => 'thumb')
          .thumbnail{:style => 'width: 100px; float: left; margin-right: 5px; height: 67px'}
            .thumb{:data => {:attribute => 'interior', :place => place}}
              = image_tag(image[1])
            .btn.remove_asset.btn-danger.btn-mini{:style => 'position: relative; top: -20px; left: 80px',:id => image ? image[0] : '', :data => {:attribute => 'interior'}} x




