- placements = Report::EXTERIOR
.row-fluid{:id => 'under_the_hood'}

  .span8
    %ul.thumbnails
      - for place in placements[0..2]
        = render :partial => 'drop', :locals => {:object => @report, :place => place, :attribute => 'under_the_hood'}
    %ul.thumbnails
      - for place in placements[3..5]
        = render :partial => 'drop', :locals => {:object => @report, :place => place, :attribute => 'under_the_hood'}

    %ul.thumbnails
      - for place in placements[6..8]
        = render :partial => 'drop', :locals => {:object => @report, :place => place, :attribute => 'under_the_hood'}

    .span{:style => "height: 96px; overflow-x: scroll; width: 98%; overflow-y: hidden;border: 1px solid #08C"}
      .imgbox#under_the_hood{:style => "height: 110px; width: #{@report.images(:group => 'under_the_hood', :size => 'thumb').size*116}px"}
        - for image in @report.images(:group => 'under_the_hood', :size => 'thumb')
          .thumbnail{:style => 'width: 100px; float: left; margin-right: 5px; height: 67px'}
            .thumb{:data => {:attribute => 'under_the_hood', :place => place}}
              = image_tag(image[1])
            .btn.remove_asset.btn-danger.btn-mini{:style => 'position: relative; top: -20px; left: 80px',:id => image ? image[0] : '', :data => {:attribute => 'under_the_hood'}} x




  .span4
    =render :partial => 'uploader', :locals => {:attribute => 'under_the_hood'}


