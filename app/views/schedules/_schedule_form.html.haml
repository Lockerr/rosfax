
.row
  %legend
    = "Здравствуйте! Вы всего в одном шаге от успешной записи на осмотр Rosfax на #{@schedule.hour} часов #{Russian::strftime(@schedule.date, '%d %B %Y')} . Для того, чтобы закончить запись, пожалуйста представьтесь и укажите Ваш номер мобильного телефона."

  = form_for @schedule, :html => {:class => 'form-horizontal'} do |f|
    = f.hidden_field :company_id
    = f.hidden_field :date
    = f.hidden_field :hour

    .control-group
      .control-label
        Имя
      .controls
        = f.text_field :name
    .control-group
      .control-label
        Телефон
      .controls
        = f.text_field :phone
    .control-group
      .controls
        = f.submit 'OK', :class => 'btn', :disabled => :disabled
        .btn.cancel{:disabled => :disabled} Отмена
