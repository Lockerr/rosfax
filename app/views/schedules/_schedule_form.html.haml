
.row
  %legend
    = "Здравствуйте! Вы всего в одном шаге от успешной записи на осмотр Rosfax на #{@schedule.hour} часов #{Russian::strftime(@schedule.date, '%d %B %Y')} . Для того, чтобы закончить запись, пожалуйста представьтесь и укажите Ваш номер мобильного телефона."

  = form_for @schedule,remote: true, :html => {:class => 'form-horizontal'} do |f|
    = f.hidden_field :company_id
    = f.hidden_field :date
    = f.hidden_field :hour

    .control-group
      .control-label
        Имя
      .controls
        = f.text_field :name
    .control-group
      .control-label
        Телефон
      .controls
        = f.text_field :phone
    .control-group
      .controls
        = f.submit 'OK', :class => 'btn', :disabled => :disabled
        = link_to 'Отмена', root_path, {:disabled => :disabled, class: :btn}

.modal#schedule-success.hide.fade
  .modal-header
    %h3 Запись на сомотр
  .modal-body
    %p
  .modal-footer
    = link_to 'Закрыть', root_path, class: 'btn btn-primary'
